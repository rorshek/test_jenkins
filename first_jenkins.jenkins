#!groovy

properties([disableConcurrentBuilds()])


pipline {
	agent {
		label 'master'
	}
	options {
		timestamps()
	}

	stages {
		stage("docker image") {
			steps {
				echo "Creating image"
				dir ('docker/toolbox') {
					sh 'docker build . '
				}

			}
		}
	}
}